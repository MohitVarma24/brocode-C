#include <stdio.h>
int main()
{
    int a = 6;
    int b = 9;
    int add = a + b;
    int sub = a - b;
    int multiply = a * b;

    printf("\n the sum of a & b is = %d", add);
    printf("\n the sum of a & b is = %d", sub);
    printf("\n the product of a & b is = %d", multiply);

    return 0;
}