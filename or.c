#include <stdio.h>
int main(){
    float temp = 10;
    if (temp <= 0 || temp >= 40){
    printf("The temperature is perfect!");
    }
    else{
        printf("The temperature is BAD!");
    };
return 0;
}